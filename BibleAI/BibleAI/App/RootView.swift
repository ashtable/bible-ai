import SwiftUI

/// Placeholder root view. Navigation (`AppRouter` + `MainTabView`, Tasks 3–4)
/// and the SwiftData `.modelContainer` (Task 5) are wired in later — keep this
/// minimal so the skeleton builds and launches without those dependencies.
struct RootView: View {
    var body: some View {
        Text("Bible AI")
    }
}
